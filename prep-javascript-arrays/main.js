const languages = ['html', 'css', 'javaScript', 'react', 'node', 'postgres'];
console.log('the value of languages is :', languages);

const firstElement = languages[0];
console.log('the value of firstElement is :', firstElement);
const thirdElement = languages[2];
console.log('the value of thirdElement is :', thirdElement);
const length = languages.length;
console.log('the value of length is :', length);
const lastElement = languages[length - 1];
console.log('the value of lastElement is :', lastElement);
